const Sequelize = require('sequelize');
// 관계형 데이터베이스(RDB)의 데이터를 조작하게 하는 기술
module.exports = class User extends Sequelize.Model {
    statis init(sequelize) {
        return super.init({

        });
    }
}